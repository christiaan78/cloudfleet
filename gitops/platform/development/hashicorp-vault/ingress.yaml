apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: vault
  namespace: platform
spec:
  ingressClassName: tailscale
  defaultBackend:
    service:
      name: hashicorp-vault-ui
      port:
        number: 8200